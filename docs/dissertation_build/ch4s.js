const { P, CH, H2, H3, cap, fcap, IMG, T, BREAK } = require('./lib');
const c = [];
c.push(...CH(4, 'Design and Implementation'));

c.push(P('This chapter describes how the design in Chapter 3 was built, and the problems encountered in building it. The problems are included deliberately: every one of them produced a silently wrong result rather than an error message, and the verification steps added in response are part of what makes the reported figures trustworthy.'));

c.push(H2('4.1  System architecture'));
c.push(P('The system is a set of standalone scripts rather than a library. Each performs one stage of the pipeline, can be run on its own, and begins by importing the shared configuration module.'));
c.push(IMG('architecture.png', 560, 335));
c.push(fcap('4.1', 'System architecture. The configuration module is shared by every stage; the analysis scripts read saved predictions rather than re-running inference.'));
c.push(P('One decision shapes everything else. Every path, hyper-parameter, split ratio, class name and augmentation setting lives in one configuration module, which validates itself on import, so changing an experimental setting means editing one file and an inconsistent configuration stops the run immediately instead of producing a result that looks plausible.'));
//@TABLE 4.1
c.push(P('A second decision follows from the first. Evaluation writes every per-image prediction to disk with its full probability vector, not just the predicted class, which makes the analysis scripts cheap: the paired tests, the triage audit, the threshold sweep and the out-of-distribution test all read those files and need neither a GPU nor TensorFlow, so any policy layered on the classifier can be re-tuned in seconds rather than by repeating inference.'));

c.push(H2('4.2  Data pipeline and training'));
c.push(P('Training implements the two stages as one script parameterised by architecture, so both models follow the same code path, which is what makes the comparison controlled in practice rather than only in intention. Evaluation loads the saved model, scores the test split once, and writes metrics, confusion matrices, Grad-CAM overlays and the per-image prediction file. Calibration fits the temperature on validation logits, measures calibration error before and after, verifies that no prediction changed, and selects the abstention threshold. The comparison script refuses to run if the two prediction files do not describe the same images, because two files from different splits would silently invalidate every paired test. The threshold sweep evaluates candidates by calling the same triage function the prototype uses rather than reimplementing the rule, since a second implementation could drift from the one that ships.'));

c.push(H2('4.3  The prototype'));
c.push(P('What the proposal committed to at this stage was a prototype interface demonstrating the trained model, not a deployable mobile application, and that is what was built. The interface is implemented in Streamlit and runs in a browser, which keeps the demonstration in the same environment as the rest of the pipeline and lets it load the trained model directly. Packaging for a phone is a deployment exercise that the efficiency measurements in Chapter 5 are intended to inform, and it is listed as future work rather than claimed here.'));
c.push(H3('4.3.1  Screening tab'));
c.push(P('The user uploads a photograph or selects one from the test split. It is preprocessed exactly as in training, scored, and the probabilities temperature-scaled. The result is a structured report whose order is a design decision: the recommended action and its timeframe appear first, in a coloured banner, before the predicted condition, because somebody reading this on a phone may not read past the first line and the first line is what they should do. The predicted class, calibrated confidence, the reason for the recommendation, the guidance for that condition and the text stating what should send them to seek care regardless follow beneath, with a Grad-CAM overlay below that.'));
c.push(P('Several details are constrained deliberately. Confidence is never displayed as 100 percent, since a rounded 0.9997 shown as certainty overclaims. When the model abstains no condition is named at all, rather than a low-confidence guess being shown with a caveat. When a result has been escalated the guidance shown is that of the escalating condition. And the report is deliberately not styled to resemble a clinician report, with no signature, name or letterhead, because a document that could be mistaken for a medical record would be a worse outcome than an unattractive one.'));
c.push(H3('4.3.2  Batch evaluation tab'));
c.push(P('The second tab scores either the project test split or a compressed archive of the user photographs organised into folders by class, reporting accuracy, per-class precision, recall and F1, a confusion matrix, and the proportion answered rather than abstained. Because the sets scored here are small it reports a confidence interval alongside the accuracy and warns below thirty images: nineteen correct out of twenty displays as 95 percent but is equally consistent with a true accuracy of 76 percent.'));
c.push(P('It also reports every image individually, carrying the filename through from the archive, and writes that table to a file alongside the aggregate metrics, with the full calibrated probability vector per row. On a small set the aggregate accuracy is the least informative part of the result, and preserving filenames is what allows a paired design such as the one in Section 5.9 to be analysed as pairs. It is also what caught a labelling error in that collection after the fact.'));

c.push(H2('4.4  Problems encountered and how they were resolved'));
c.push(P('Seven problems during implementation produced a wrong result that looked correct rather than an error message. They are listed with their causes and fixes in Appendix E, and two are worth noting here. The border ablation appeared to prove robustness on a fixture where it could not discriminate at all, which is why the equal-area interior control and the explicit floor check were added. And a screening report headed URGENT whose advice read that no concerning features were found was found by rendering the report rather than by reading the code: the classifier and the triage rules were each behaving correctly, and only the combination was wrong.'));

c.push(H2('4.5  Summary'));
c.push(P('The implementation follows the design without deviation. Its two structural decisions, a single shared configuration and saved per-image probability vectors, are what allowed the analysis in Chapter 5 to be re-run and re-audited cheaply. The problems, listed in full in Appendix E, were all of one kind, silent wrong answers rather than error messages, and each fix is a check rather than a correction.'));

module.exports = c;
