const { P, CH, H2, H3, cap, fcap, IMG, T, BREAK } = require('./lib');
const c = [];
c.push(...CH(3, 'Methodology'));
c.push(P('This chapter sets out the experimental design. The central requirement is that the comparison between the two architectures is controlled: both are trained on the same images, with the same split, preprocessing, augmentation, class weights and training procedure, so that any difference between them is attributable to the architecture rather than to the conditions of the experiment.'));


c.push(H2('3.1  Research design'));
c.push(P('The study is a quantitative computational experiment. Two architectures are trained by transfer learning on a four-class dataset assembled from public sources, evaluated on a held-out test set, and compared using paired statistical tests. Alongside accuracy it measures model size and inference latency, calibration, behaviour under abstention, and whether either model is exploiting dataset artefacts. The test split is read exactly once, at the end; every development decision was made on validation, which is what allows the reported figures to be treated as an estimate of performance on unseen data.'));

c.push(H2('3.2  Datasets'));
c.push(P('Three public image collections were used. No new clinical data was gathered and no patient-identifiable information was handled at any point. The healthy and wound images come from the Lower Limb and Feet Wound Image Dataset on Mendeley Data (Islam, 2026), the nail images from the onychomycosis dataset published on figshare by Han (2017) and described in Han et al. (2018), and the foot ulcer images from the dataset released by Wang et al. (2020).'));
//@TABLE 3.1

c.push(H2('3.3  Data preparation'));
c.push(P('Exact duplicates were removed by file hash, because a duplicate appearing in both training and test would inflate the reported accuracy. The figshare healthy nail images are montage sheets rather than individual photographs, so they were tiled. Tiling produced 18,096 images from roughly twenty sheets, far more than the other classes and far less diverse than the count suggests, so the contribution was capped at 1,000 sampled evenly across all sheets, with tiles from one sheet grouped so that none straddles the train and test boundary.'));
//@TABLE 3.2

c.push(H2('3.4  Splitting the data'));
c.push(P('The data was split 70 / 15 / 15 into training, validation and test, stratified by class and by source. Stratifying on class alone is not sufficient here, and an earlier version of this work demonstrated why: because each class draws from more than one source, class-only stratification sent every montage-tile group into training and left the test set with no healthy nail images at all, so healthy-nail performance was silently unmeasured. Stratifying on the class and source combination fixes this, and the preprocessing script now fails if any source is missing from any split.'));
//@TABLE 3.3

c.push(H2('3.5  Preprocessing'));
c.push(P('Images are resized to 224 by 224 pixels with the aspect ratio preserved and the result centre-cropped. Stretching would distort the 153 images with extreme aspect ratios, and padding to a square would reintroduce a black border of exactly the kind removed from the ulcer images, in which 25 to 32 percent of the frame was pure black padding present in no other class. That padding was cropped before resizing, and the residual measured at 0.000.'));
c.push(P('Normalisation is deliberately not applied at this stage. MobileNetV2 expects inputs in the range minus one to one and ResNet50 caffe-style mean subtraction, so baking either into the stored images would handicap one architecture and invalidate the comparison. Each model applies its own inside its own graph, verified numerically to within two parts in one hundred thousand.'));
c.push(P('Randomised blur is included for a reason specific to this dataset: healthy nail tiles are upscaled from roughly 102-pixel crops while the onychomycosis images are downscaled from several hundred pixels, so sharpness correlates with class and could act as a shortcut.'));

c.push(H2('3.6  Augmentation'));
c.push(P('Augmentation is applied to the training split only, on the fly, using preprocessing layers inside the data pipeline. Keras 3 removed the image data generator used in older tutorials, so augmentation is implemented with these layers instead.'));
//@TABLE 3.4

c.push(H2('3.7  Model architectures and transfer learning'));
c.push(P('Two architectures were compared. MobileNetV2 (Sandler et al., 2018) is the deployment candidate, chosen for its small size and its depthwise separable convolutions. ResNet50 (He et al., 2016) is the accuracy reference: substantially larger, more conventional, and widely used in the studies reviewed in Chapter 2. Both were initialised with ImageNet weights, since 8,374 images is orders of magnitude short of what training from scratch requires. The classification head is identical for both: global average pooling, dropout at 0.3, and a four-unit dense layer with softmax and L2 regularisation.'));
c.push(IMG('twostage.png', 560, 190));
c.push(fcap('3.1', 'Two-stage transfer learning. The backbone is frozen while the head is trained, then fine-tuned at a learning rate one hundred times lower.'));
c.push(P('Training proceeds in two stages. First the backbone is frozen and only the new head is trained, because a randomly initialised head produces large gradients that would destroy the transferred features. Then the upper backbone layers are unfrozen and trained at a learning rate one hundred times lower, adapting the general features to this domain without discarding them. Batch normalisation layers stay frozen throughout, since their statistics come from ImageNet and re-estimating them from small medical batches destabilises training. One implementation detail is worth recording: the second stage passes the first stage best validation loss to the checkpoint callback as its baseline, because otherwise the callback treats its own first epoch as best and can overwrite better weights with worse.'));
//@TABLE 3.5

c.push(H2('3.8  Class imbalance and evaluation metrics'));
c.push(P('The classes are imbalanced, and the imbalance runs the wrong way clinically, since foot ulcer is among the smallest, so class weights inversely proportional to frequency were applied to the loss. For the same reason accuracy alone is not reported: the primary metrics are macro-averaged F1 and per-class recall, with particular attention to ulcer recall, because a missed ulcer is the costly error and overall accuracy can stay high while that recall falls.'));

c.push(H2('3.9  Testing for dataset shortcuts'));
c.push(P('Each class draws largely from one source, so a model could in principle score well by recognising the dataset rather than the condition. Section 2.5.8 identifies this as a weakness published work generally leaves unaddressed, so it was tested three independent ways. The first measures accuracy within a single source: mendeley_foot contributes both healthy feet and wounds, photographed at the same clinic with the same equipment, so on that comparison knowing the dataset tells a model nothing. The second applies Grad-CAM (Selvaraju et al., 2017) to see where each model attends, on misclassified predictions as well as correct ones. The third is a controlled occlusion ablation.'));
c.push(P('The ablation design is worth setting out carefully. Masking the border and observing that accuracy holds proves nothing on its own, because masking anything removes information. The test is interpretable only against an equal-area interior control: the same number of pixels, the same grey occlusion, differing only in location. A fourth arm masks the interior and leaves only the border, to measure what the frame alone carries, and a floor check detects the case where every arm collapses to chance.'));

c.push(H2('3.10  Calibration, selective prediction and the referral policy'));
c.push(P('A softmax score is not a probability. Temperature scaling (Guo et al., 2017) was fitted on the validation logits by minimising negative log likelihood, and expected calibration error measured over fifteen bins before and after; a single temperature rescales confidence without changing the ranking of the classes, which was verified. From the calibrated probabilities a risk-coverage curve was computed and an abstention threshold selected as the lowest confidence at which accuracy among answered cases reaches 99 percent. Below it the system declines to name a condition and refers the case instead.'));
c.push(P('The classifier reports appearance; the prototype has to recommend an action, and the two errors are not equally costly. A false referral wastes an appointment, while telling somebody with an ulcer that nothing is wrong can cost them a foot. The policy is therefore deliberately asymmetric: each class carries a fixed clinical band, an inconclusive result is raised to a routine referral rather than falling back on the most likely class, and a serious condition holding meaningful probability escalates the band even when it is not the top prediction, with the escalating condition owning the advice shown. Nothing here is learned; the bands and thresholds are a human decision recorded in the configuration module.'));

c.push(H2('3.11  Statistical analysis'));
c.push(P('Both models are evaluated on the same images, so their errors are correlated and treating the two accuracy figures as independent samples would overstate any difference. The comparison is pairwise, using McNemar’s exact test on the per-image outcomes, in which only the images the models disagree on carry information; the exact form was used because the number of disagreements is around twenty, where the chi-square approximation is least reliable. A paired bootstrap interval with 10,000 resamples accompanies it, drawing images rather than models so that each pair stays together, and answers how large a difference could plausibly be. The same test was applied within each class and source, and proportions are reported with Wilson intervals rather than the normal approximation, which misbehaves near zero and one and on small counts.'));

c.push(H2('3.12  Validation beyond the source datasets'));
c.push(P('Everything above is measured on images from the three source datasets. Two further tests probe the two ways a real photograph can differ from those: it can show one of the four conditions but have been captured differently, or it can show a condition the system was never taught.'));
c.push(H3('3.12.1  Photographs captured outside the source datasets'));
c.push(P('A small set of photographs was collected on mobile phones and scored through the prototype batch tab in the deployment configuration: MobileNetV2, temperature scaling, and the abstention threshold applied. The collection is a paired comparison rather than a sample. Each subject was photographed twice, once against a plain background and once against a patterned one, on the same handset and under the same lighting, so the background is what differs within a pair; on a set this size a paired test is far stronger than comparing two independent groups. Two limits were accepted in advance rather than presented later as caveats: the set is small, so it establishes the direction of a difference but not its size, and the available subjects had healthy feet, so it measures how often a healthy foot is misread but not how often a real condition is missed.'));
c.push(H3('3.12.2  A condition outside the four classes'));
c.push(P('The 578 nail dystrophy images excluded during preprocessing were scored with the same configuration. They were excluded before the split was made, so no model has seen them at any stage, and they are real clinical photographs rather than artificial inputs. What was measured was their confidence against the test set, the proportion declined, the classes assigned and the referral bands produced: the first two ask whether the system can tell it is out of its depth, the last two what a person photographing this condition would be told.'));

c.push(H2('3.13  Tools, reproducibility and ethics'));
c.push(P('The system was implemented in Python using TensorFlow 2.16 with Keras 3, scikit-learn, SciPy, Pillow, NumPy, Matplotlib and Streamlit, and trained in Google Colab on an NVIDIA Tesla T4. The proposal listed OpenCV among the required resources; it was not needed, and this is recorded rather than left as an unexplained discrepancy.'));
c.push(P('Every path, hyper-parameter, split ratio, class name and augmentation setting is held in one configuration module that every script imports and that validates itself on import, so an inconsistent configuration fails immediately. A fixed seed of 42 governs the split and initialisation, and all per-image predictions are written to disk with their full probability vectors, so the analysis and the referral policy can be re-examined without repeating inference.'));
c.push(P('All image data comes from public research datasets used in accordance with their terms. They contain photographs of feet and nails only, with no faces and no identifying information, and no attempt was made to re-identify anyone. The system is positioned as an assistive screening aid rather than a diagnostic device, and this is enforced in the prototype rather than merely stated: it shows a disclaimer with every result, declines to name a condition when confidence is low, and always directs the user towards a healthcare professional.'));

c.push(H2('3.14  Summary'));
c.push(P('The design is a controlled comparison: two architectures trained under identical conditions on one split that is read once, at the end. Beyond the classification itself it includes three tests of whether the models are reading pathology or dataset provenance, a calibration and abstention procedure that makes the reported confidence meaningful, a referral policy deliberately biased towards sending people to a clinician, and two tests of behaviour outside the source datasets. Chapter 4 describes how this design was implemented, and the problems encountered in implementing it.'));

module.exports = c;
