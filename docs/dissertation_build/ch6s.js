const { P, CH, H2, H3, cap, T, BREAK } = require('./lib');
const c = [];
c.push(...CH(6, 'Discussion'));

c.push(H2('6.1  Meeting the research objectives'));
//@TABLE 6.1
c.push(P('All five objectives were met and two exceeded. Objective 4 asked for standard metrics; the study added paired statistical testing, which the reviewed literature rarely applies. Objective 5 asked for a prototype; the one delivered also calibrates its confidence, declines to answer when uncertain, and turns a classification into a recommended action with a timeframe.'));

c.push(H2('6.2  Which architecture should be recommended'));
c.push(P('MobileNetV2 is recommended for phone-based rural screening on four measured grounds: no significant accuracy difference between the two models, with any real difference bounded at 1.4 points and the two indistinguishable on the confound-free comparison; a tenth of the size and roughly twice the CPU speed; greater robustness to dataset artefact, recovering 62 of 692 non-healthy images from the border alone against 193; and a working abstention threshold, which ResNet50 did not need and therefore does not have. The contribution is the quantified trade-off rather than the verdict.'));
c.push(H3('6.2.1  The case against, stated fairly'));
c.push(P('ResNet50 missed three of the 199 test ulcers against nine, the largest effect in the study and the closest to significance at p = 0.0703, and it should not be dismissed. Two things qualify it. Failing to establish a difference is not establishing equivalence, and on 199 ulcers this study cannot settle six images either way. And the response is a design response rather than an argument: the referral policy brings seven of the nine misread ulcers into the urgent band, none of which would reach it otherwise. A further consideration runs in MobileNetV2’s favour, since it ran out of epoch allowance while ResNet50 stopped on convergence, so the measured gap is if anything an over-estimate.'));

c.push(H2('6.3  What the tests for dataset shortcuts establish'));
c.push(P('The three tests bound the confound rather than eliminating it. They establish that both models score close to 99 percent where provenance cannot help, that attention falls on the toes and nail plates, and that masking the border costs a small fraction of what masking the same area of interior costs. They do not establish that no source-specific information contributes anything, which no test on data with this structure could show. The border-only arm also separates the architectures in a way relevant to any future work scaling the backbone up: the model with ten times the parameters recovered about three times as much class information from the frame alone.'));
c.push(P('The three tests bound the confound rather than eliminating it, and the distinction matters. They establish that both models score close to 99 percent where provenance cannot help, that attention falls on the toes and nail plates, and that masking the border costs a small fraction of what masking the same area of interior costs. They do not establish that no source-specific information contributes anything, which no test on data with this structure could show. The border-only arm also separates the architectures in a way relevant to any future work scaling the backbone up: the model with ten times the parameters recovered about three times as much class information from the frame alone, so capacity and artefact absorption move together.'));

c.push(H2('6.4  Generalisation beyond the source datasets'));
c.push(H3('6.4.1  The drop in accuracy is the finding'));
c.push(P('Accuracy on ten phone photographs taken outside all three source datasets was 6 of 10, against 98.32 percent on the test set. It should be read as a quantified gap rather than a failure of the work. Section 2.5.8 identified absent external validation as a recurring weakness in the published studies, and a study reporting only held-out accuracy leaves a reader unable to judge whether that figure survives contact with a real photograph; the distance between these two numbers is exactly the information the reviewed work does not provide.'));
c.push(P('Ten images support a direction, not a magnitude; the interval from 31.3 to 83.2 percent spans figures that would be tolerable and figures that would be unusable. Two features of the errors matter more than their count. All four were healthy feet read as wounds, the direction the referral policy tolerates, so nobody was wrongly reassured. But every subject had healthy feet, so the set cannot measure a missed diagnosis, which is the error the whole policy is organised around.'));
c.push(H3('6.4.2  Acquisition conditions the user does not know matter'));
c.push(P('Every plain-background photograph was correct and four of five patterned ones wrong, all four discordant pairs pointing the same way at p = 0.1250 where the best attainable value is 0.0625. The mechanism is consistent with Section 6.3: a patterned background puts edge and texture structure around the foot, which the border-only ablation showed both models can read to some degree, and the abstention threshold responded to the same thing. Any deployment would therefore need guidance on how to take the photograph, and that guidance is part of the system rather than incidental to it.'));
c.push(H3('6.4.3  What the abstention mechanism protects against'));
c.push(H3('6.4.4  What this costs the referral policy'));
c.push(P('On the test set the policy was effectively free, referring no healthy and no nail fungal case for urgent or prompt care, so it appeared to buy seven recovered ulcers at no cost to well people. The external photographs show that accounting holds only on data resembling the source datasets: at least six of those ten healthy subjects are routed to a clinician. The policy protects against the dangerous error by sending a substantial fraction of healthy phone photographs to a clinician, and where clinician time is the scarce resource, which is the setting this project is motivated by, that cost is real and must be weighed rather than reported as zero. It also indicates where the system belongs: a triage aid deciding who is seen sooner, not a filter deciding who is not seen at all.'));
c.push(H3('6.4.5  A condition the system was not taught'));
c.push(P('All 578 dystrophy images were assigned to a nail class and none to a foot class, so the model identifies which part of the body it is looking at, and 97.8 percent were labelled nail fungal infection, the nearest available condition. The outcome is mostly benign, with 98.4 percent routed to a clinician who would arrive at the correct name, but the reason deserves stating exactly, because it is easy to present as a success the design did not earn. What limits the harm is that the nearest label in this class set happens to carry a referral. Had the excluded condition most resembled the healthy class, the same behaviour would have told all 578 people that nothing was wrong; nine were told that as it is. That is a property of the class structure rather than a safeguard, and a deployed system would need a genuine rejection mechanism.'));

c.push(H2('6.5  Relationship to the reviewed literature'));
c.push(P('The accuracy figures sit within the range reported by the reviewed studies, but direct comparison would mislead: those studies use different datasets, different numbers of classes and different definitions of the same class, and an accuracy figure is interpretable only alongside the dataset that produced it. The contribution is therefore better positioned as methodological. Section 2.5.8 identified four gaps, and this study addresses all four: identical training conditions and a paired test, size and latency measured on the device class that matters, Grad-CAM applied to misclassified as well as correct predictions, and photographs from outside all three source datasets scored through the same pipeline with the drop reported rather than omitted. The occlusion design has the clearest claim to methodological novelty here.'));

c.push(H2('6.6  Limitations'));
c.push(P('The healthy nail images derive from roughly twenty photographic sessions rather than 18,096 independent observations, and grouping tiles by sheet cannot create diversity the source lacks. Resolution correlates with class among the nail images, mitigated by randomised blur but not removed. Class is partly confounded with source by design, bounded by the three tests in Section 6.3 rather than eliminated. The nail images are predominantly fingernails while the target condition is predominantly a toenail one.'));
c.push(P('Every result rests on one partition and one training run of each architecture at a single seed. Several differences discussed are a handful of images wide, smaller than the variation repeated runs would show, so results hold on this split rather than establishing a stable ordering. The referral thresholds were swept on validation, but the sweep’s cost budget never bound at any value tested, so the rule reduced to maximising benefit with nothing pushing back; the adopted threshold is bounded and defensible but not fully tuned.'));
c.push(P('The system has no reject class, so a photograph of something that is not a foot is classified as a foot condition, and Section 6.4.3 sets out why no threshold on confidence could prevent that. The clinical guidance attached to each class is fixed text rather than an assessment of severity, which the model has no basis for, having never seen infection status, wound depth, circulation or patient history; it should be reviewed against current guidelines before the system is shown to any clinician.'));
c.push(P('No data from a rural or field setting was used at any stage, so this work is motivated by rural screening rather than validated for it, and the distinction is not merely careful wording: the conditions differing between a clinic and a field setting are exactly those Section 6.4 shows the model to be sensitive to. The external validation is itself small, with camera, lighting and operator all confounded with the single fact that these photographs did not come from the source datasets. It establishes that a gap exists and gives its direction, not its size, its cause, or the error that matters most.'));


module.exports = c;
